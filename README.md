# mother-agent-system-
Mother Agent is an LLM-powered orchestrator that decomposes software specs into subtasks, runs agents serially to build a  project, validates output with a supervisor, and retries the full batch on failure — all via Ollama/Gemini/OpenAI APIs.
